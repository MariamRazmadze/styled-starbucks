import Modal from "react-modal";
import styled from "styled-components";

export const NavbarModal = styled(Modal).attrs({
  style: {
    overlay: {
      backgroundColor: "transparent",
    },
    content: {
      color: "lightsteelblue",
    },
  },
})``;
